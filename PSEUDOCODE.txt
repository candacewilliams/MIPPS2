READ IN STRING (ALLOCATE 1001 SPACES: 1000 FOR STRING, 1 FOR NULL
GET SUBSTRING (READ STRING UNTIL ',' , NULL , OR '\n')
	CHECK FOR BLANK SUBSTRING (ALL SPACES)
		IF YES, DISPLAY NAN
		IF NO, 
			CHECK IF VALID HEX && LESS OR EQUAL TO 8 CHARS
				IF YES, DISPLAY UNSIGNED INT
			CHECK IF VALID HEX && GREATER THAN 8 CHARS
				IF YES, DISPLAY "TOO LARGE"
			IF INVALID HEX: DISPLAY NAN
	